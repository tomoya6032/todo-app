= link_to board_task_path(@board, task) do
  .card
    - if task.eyecatch.attached? 
      .card_image
        = image_tag task.eyecatch
    .task_content
      .task_title
        = task.title
      .task_content
        = task.content
        
      .card_detail
        
        = image_tag task.user.avatar_image 
        
        %div
          %p= task.author_name
          %p= task.display_created_at

        .comment_count
          %p コメント数
          = task.comment_count

          .comment_detail
            -# - @comments.each do |comment|  
            -#   - if @comment.avatar_image?
            = image_tag @comment.avatar_image 
  
            -# = image_tag current_comment.avatar_image
            -# = image_tag comment.image.url
            -# = image_tag @comments.user.avatar_image
  

            -# = image_tag current_user.avatar_image

            -# = image_tag @comments.comment_image

        