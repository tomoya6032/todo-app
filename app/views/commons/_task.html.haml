= link_to board_tasks_path(task) do
  .card
    -# - if board.eyecatch.attached? 
      .card_image
        -# = image_tag board.eyecatch
    .card_content
      .card_title
        = task.title
      -# .card_heart
      -#   = image_tag 'heart.svg'
      -#   %span= board.like_count 
    -#   .card_detail
    -#     = image_tag board.user.avatar_image 
        %div
          %p= task.author_title
          %p= task.display_created_at